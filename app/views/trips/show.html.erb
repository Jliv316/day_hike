<%= @trip.name %>
<%= @trip.total_distance %>
<%= @trip.average_distance %>
<%= @trip.max_distance %>
<%= @trip.min_distance %>

<% @trails.each do |trail| %>
  <%=link_to "#{trail.name}", "/trails/#{trail.id}" %>
  <%= trail.address %>
  <%= trail.length %>
<%end%>